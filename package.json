{
  "name": "proker-monorepo",
  "private": true,
  "scripts": {
    "mobile": "turbo run start --filter=mobile",
    "dev": "turbo run start",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.4.0"
}
