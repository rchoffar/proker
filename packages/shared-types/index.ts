// Shared TypeScript types between mobile app and future API
// When types are needed in both apps/mobile and apps/api, define them here

export type Currency = 'EUR' | 'USD' | 'GBP';
export type SessionType = 'tournament' | 'cash';
export type GameType = 'NLH' | 'PLO' | 'other';
